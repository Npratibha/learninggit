2.SELECT WORKER_ID,DEPARTMENT,FIRST_NAME,LAST_NAME
FROM WORKER_DETAILS
WHERE SALARY IN (
                  SELECT MAX(SALARY)
		 FROM WORKER_DETAILS
		GROUP BY DEPARTMENT);

1.SELECT *
from Worker_Details
where SALARY IN(SELECT SALARY
               
				FROM Worker_Details
				WHERE WORKER_ID
				NOT IN 
				    (SELECT
				    MAX( WORKER_ID)
				    FROM Worker_Details
				    GROUP BY SALARY));